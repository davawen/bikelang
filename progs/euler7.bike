func is_prime(i32 n) -> bool {
	i32 idx = 2;
	loop {
		if n % idx == 0 { return 0 == 1 };
		if idx >= n/2 { return 1 == 1 };

		idx = idx + 1;
	};
}

func main() -> void {
	i32 idx = 1;
	
	loop {
		if idx > 100 { break };

		if is_prime(idx) {
			print#(idx, ", ");
		};

		idx = idx + 1;
	};
}
