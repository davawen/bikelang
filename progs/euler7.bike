func is_prime(i32 n) -> bool {
	i32 idx = 2;
	loop {
		if n % idx == 0 { return false };
		if idx >= n/2 { return true };

		idx = idx + 1;
	};
}

func main() -> void {
	i32 idx = 1;
	
	loop {
		if idx > 100 { break };

		if is_prime(idx) {
			print#(idx, ", ");
		};

		idx = idx + 1;
	};
}
